declare module 'xlsx-style' {
  // Importing the original xlsx types for better type support
  import * as XLSX from 'xlsx';

  // Extend the original types as needed or use any for simplicity
  export = XLSX;
}
